#comics.transitions-enabled
	- @comics.each do |comic|
		.box.panel.panel-default
			= link_to (image_tag comic.image.url), comic
			.panel-body
				%h2= link_to comic.title, comic
				%p.user
				Submitted by 
				= comic.user.email